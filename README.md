Telemetry Logger

The .py file writes random data that may resemble telemetry, however the data is completely random so it's not logging any kind of route. All data is written to the .csv file called "telemetry_log.csv" 

There isn't much else to say
